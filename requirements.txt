# VHDLens — Python 의존성
# 설치: pip install -r requirements.txt
# (가상환경 권장: python -m venv .venv && .venv\Scripts\activate)

# 테스트
pytest==9.0.2

# Tree-sitter: AST 기반 VHDL 파싱 (REQ-02, REQ-09)
tree-sitter==0.25.2
tree-sitter-vhdl==1.3.1

# (추후) 시각화
# graphviz>=0.20.0
